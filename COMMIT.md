```
feat: Warsha v1.3 — real terminal, virtual FS, Islamic themes

بسم الله الرحمن الرحيم

Dual-mode: LOCAL (virtual FS) + LIVE (real bash via xterm.js + Python).
14 Islamic themes, tmux keybindings, 221 tests, fullscreen, Amiri calligraphy.
```

**Usage:**

```bash
git add .
git commit -F COMMIT.msg
```
